{{#paper-dialog}}
    {{#paper-dialog-content}}
    <h1 class="md-title">{{ model.name }}</h1>
    {{paper-input
          type="number"
          label="Peso"
          value=weight
          onChange=(action (mut weight))
        }}
    {{/paper-dialog-content}}

    {{#paper-dialog-actions class="layout-row"}}
        {{#paper-button onClick=(route-action "cancelRegister")}}Cancelar{{/paper-button}}
        {{#paper-button onClick=(route-action "addRegister" weight) }}Guardar{{/paper-button}}
    {{/paper-dialog-actions}}
{{/paper-dialog}}
